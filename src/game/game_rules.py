# src/game/game_rules.py

def is_checkmate(board, color):
    """
    Check if the given player is in checkmate.
    
    Args:
    - board: 2D list representing the board.
    - color: 'white' or 'black'.
    
    Returns:
    - True if it's checkmate, False otherwise.
    """
    # Implement checkmate logic (this is a simplified version).
    return False

def is_castling_possible(board, start_pos, end_pos, color):
    """
    Check if castling is possible for the current player.
    
    Args:
    - board: 2D list representing the board.
    - start_pos: Tuple (row, col) of the piece.
    - end_pos: Tuple (row, col) of the move destination.
    - color: 'white' or 'black'.
    
    Returns:
    - True if castling is possible, False otherwise.
    """
    # Implement simplified castling logic (needs full implementation for the edge cases).
    return True
